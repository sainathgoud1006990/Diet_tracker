{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm install && NODE_ENV=production BASE_PATH=/ PORT=3000 pnpm --filter @workspace/diet-tracker run build",
  "outputDirectory": "artifacts/diet-tracker/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
